# @resilireplay/mcp-chaos

Authorized stdio, Streamable HTTP, and backwards-compatible SSE reliability testing using the
official MCP TypeScript SDK. Includes a typed, value-redacting importer for reviewed MCP Inspector
2.0.0 `mcp.json` files.
