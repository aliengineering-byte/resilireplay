name: Publish npm package

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_ref:
        description: Existing v-prefixed release tag to publish
        required: true
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    if: >-
      github.repository == 'aliengineering-byte/resilireplay' &&
      (github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'release' && startsWith(github.event.release.tag_name, 'v')))
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_ref || github.event.release.tag_name }}
      - uses: pnpm/action-setup@v6
        with:
          version: 10.14.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
      - name: Verify release tag and package version
        shell: bash
        env:
          RELEASE_REF: ${{ github.event_name == 'workflow_dispatch' && inputs.release_ref || github.event.release.tag_name }}
        run: |
          package_version="$(node --print "require('./packages/cli/package.json').version")"
          if [[ "v${package_version}" != "${RELEASE_REF}" ]]; then
            echo "Tag ${RELEASE_REF} does not match resilireplay ${package_version}." >&2
            exit 1
          fi
      - run: pnpm install --frozen-lockfile
      - run: pnpm quality
      - run: pnpm demo:studio
      - name: Publish with npm Trusted Publishing
        run: npm publish ./packages/cli --access public
