# Generated causal regression

These four files were generated by public `resilireplay@0.6.0` from the synthetic
`canary-expected-failure` scenario in `stdio.campaign.yml`; they were not hand-authored. The source
trace had six sanitized events and the minimized fixture has three. `manifest.json` binds the source,
fixture, scenario, and executable test hashes.

```console
node --test regression.test.mjs
```

The canary is an inert synthetic reliability condition, not a discovered vulnerability.
